apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: {name: product-api}
spec:
  podSelector: {matchLabels: {app.kubernetes.io/name: product-api}}
  policyTypes: [Ingress]
  ingress:
    - ports: [{protocol: TCP, port: 8080}]
