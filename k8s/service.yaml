apiVersion: v1
kind: Service
metadata:
  name: product-api
  namespace: product-api
spec:
  selector:
    app: product-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
